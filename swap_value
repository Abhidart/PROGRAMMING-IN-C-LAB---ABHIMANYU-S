#include<stdio.h>
void swap(int,int); 
void main( )
{
    int a = 10, b = 20;
    printf("Before swapping");
    printf (“a=%d b=%d",a,b);
    swap(a,b);
    return 0;
}
void swap(int x,int y)
{
    int t;
    t=x;
    x=y;
    y=t;
    printf("After swapping");
    printf ( "a=%d b=%d",x,y);
}
